name: Banking

on:
  push:
      branches: 
      - Master
      
jobs:

  BuildProject:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2

        - name: build banking
          shell: bash
          run: |
            cd banking
            cmake -H. -B_build
            cmake --build _build
          
  Testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: update
        run:  git submodule update
      
      - name: test
        shell: bash
        run: |
          cmake -H. -B_build
          cmake --build _build
          cd _build
          ./RunTest
        
